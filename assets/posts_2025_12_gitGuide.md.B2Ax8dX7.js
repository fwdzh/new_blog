import{_ as s,c as a,o as t,af as e}from"./chunks/framework.DkYm0iM_.js";const o=JSON.parse('{"title":"Git 入门指北","description":"","frontmatter":{"title":"Git 入门指北","date":"2025-12-10T00:00:00.000Z"},"headers":[],"relativePath":"posts/2025/12/gitGuide.md","filePath":"posts/2025/12/gitGuide.md","lastUpdated":1765387321000}'),h={name:"posts/2025/12/gitGuide.md"};function p(l,i,n,d,k,g){return t(),a("div",null,[...i[0]||(i[0]=[e(`<h1 id="git-入门指北" tabindex="-1">Git 入门指北 <a class="header-anchor" href="#git-入门指北" aria-label="Permalink to “Git 入门指北”">​</a></h1><h2 id="git-有啥用" tabindex="-1">Git 有啥用？ <a class="header-anchor" href="#git-有啥用" aria-label="Permalink to “Git 有啥用？”">​</a></h2><p>Git 是一个很好用的代码管理工具，从这方面来说，其实 Git 也可以拿来当网盘来着。其实我也不是很了解的，不过也用了很久了，也可以小小总结一下。</p><p>网上已经有很多的教程和攻略，我主要谈谈自己的理解和印象了。Git 可以保存下来我喜欢的代码，可以同步代码方便在不同设备进行工作，可以回退到以前的版本，结合 Github pages 或 Cloudflare 可以很方便地构建静态网站。</p><h2 id="创建第一-个仓库" tabindex="-1">创建第一（？）个仓库 <a class="header-anchor" href="#创建第一-个仓库" aria-label="Permalink to “创建第一（？）个仓库”">​</a></h2><p>基于 Git 的代码托管平台主要有 Github，GitLab，Gitee，基础的操作都差不多，目前没用过 GitLab，这里主要以 Gitee 为例。</p><p><img src="https://image.wuyi.host/posts/20251210232108185.png" alt=""></p><p>首先我们创建一个仓库，点击头像左侧的加号可创建仓库，取一个仓库名称，Gitee 现在会自动生成路径。这个我不太了解，主要用 GitHub，不过感觉尽量取一样的比较好。</p><p>其他内容都不需要管，保持默认即可，可以看看是否和我这里一样，点击创建。创建成功后，会出现这样的页面。其实直接复制第一块命令和第二块，就可以完成创建和上传，但是当熟练之后其实没有必要，并且我们这里了解一些这些命令是干啥的。</p><p>以下所有出现的代码块，均为命令行命令，粘贴进命令行运行。</p><p><img src="https://image.wuyi.host/posts/20251211011134580.png" alt=""></p><h3 id="git-信息设置" tabindex="-1">Git 信息设置 <a class="header-anchor" href="#git-信息设置" aria-label="Permalink to “Git 信息设置”">​</a></h3><p>我们提交代码时，需要让 Git 知道是谁提交了代码，所以需要配置用户名和邮箱。这里的 <code>--global</code> 代表我们设置的是全局的信息。</p><p>这里其实就是上方页面的第一块的部分，如果你创建仓库后显示了着一块，直接复制即可。如果没有，把 <code>&quot; &quot;</code> 里的内容替换为你的用户名和邮箱就可。建议设置成 gitee 或 github 的用户名或邮箱，不设置成这样应该也行。</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --global</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user.name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;fwdzh&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --global</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> user.email</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;wu_yi_@outlook.com&quot;</span></span></code></pre></div><h3 id="创建仓库" tabindex="-1">创建仓库 <a class="header-anchor" href="#创建仓库" aria-label="Permalink to “创建仓库”">​</a></h3><h4 id="初始化" tabindex="-1">初始化 <a class="header-anchor" href="#初始化" aria-label="Permalink to “初始化”">​</a></h4><p>选择一个文件夹作为你的 Git 仓库，命令行里进入对应的文件夹，如果你不知道该怎么做，使用 VS Code 打开那个文件夹。</p><p>按 <code>Ctrl + \` </code> 可以打开VS Code 的终端。如果发现 <code>Ctrl + v</code> 无法在 VS Code 终端中粘贴内容，试试 <code>Ctrl + Shift + v</code>。</p><p><img src="https://image.wuyi.host/posts/20251210234448230.png" alt=""></p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span></span></code></pre></div><p>首先需要运行这个命令来初始化 Git 仓库，这会在该文件下新建一个名为 <code>.git</code> 的文件夹，但是 VS Code 和 Windows 文件管理器默认会隐藏这个文件夹。</p><h4 id="连接远程仓库" tabindex="-1">连接远程仓库 <a class="header-anchor" href="#连接远程仓库" aria-label="Permalink to “连接远程仓库”">​</a></h4><p>目前我们只创建了本地仓库，并未与远程仓库连接。</p><p>这个里面是我的 gitee 仓库的链接哈，创好仓库之后第一行就有 https 链接的，可以直接复制替换一下。或者那个第二块的代码的倒数第二行其实就是这个，复制一下即可。</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://gitee.com/fwdzh/example.git</span></span></code></pre></div><p><code>git remote add</code> 就是添加远程仓库的，origin 相当于是我们给这个远程仓库取的名字，也可以叫别的，但默认都会叫 origin。先不必管这个。</p><p>添加之后，输入</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span></span></code></pre></div><p>检查是否连上了远程仓库，如果连上了，大概会显示这样的内容。</p><p><img src="https://image.wuyi.host/posts/20251211000710260.png" alt=""></p><h4 id="提交与推送" tabindex="-1">提交与推送 <a class="header-anchor" href="#提交与推送" aria-label="Permalink to “提交与推送”">​</a></h4><p>目前我们的仓库是空的，因此需要添加一些文件。</p><p>随便创建一个文件，然后输入</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> commit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;First Commit&quot;</span></span></code></pre></div><p><code>git add</code> 命令是将文件提交到暂存区的，后面跟的是文件或文件夹的相对路径，<code>.</code> 表示的就是当前的整个文件夹。因此这个命令会将整个文件夹中的文件提交到暂存区。</p><p><code>git commit -m</code> 可以理解为给你的提交评论，说明你修改了啥东西，后面跟一个字符串，可以是任何你想写的内容。这个是必须要填的，相当于正式地提交修改，但不会同步到远程仓库。</p><p>推送代码：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> master</span></span></code></pre></div><p>只有第一次需要这样输入，因为 <code>-u</code> 会设置以后默认推送到 origin 的 master 分支，暂时不用理会这些。</p><p>若成功，应该需要输入账号密码，或在浏览器认证。</p><p><img src="https://image.wuyi.host/posts/20251211001420300.png" alt=""></p><p>若成功，大概会这样显示，我输错了，用了个 <code>git push</code>，那个失败了，不需要管。</p><p><img src="https://image.wuyi.host/posts/20251211001706746.png" alt=""></p><p>成功推送之后，回到浏览器看 gitee 仓库，里面会有刚才我们推送的文件。</p><p>以后每次修改文件推送，大概的流程就是</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> commit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;msg&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span></span></code></pre></div><p>不一定要 <code>git add .</code> ，这里举个例子，实际开发应该不推荐每次都提交整个文件夹。刚入门的话就这样用也没问题。</p><p>commit 的 msg 替换成自己想 commit 的内容。</p><p>由于我们第一次推送用的是 <code>git push -u origin master</code>，之后再提交代码只需要用 <code>git push</code> 就可以了。</p><h2 id="如何抄作业" tabindex="-1">如何抄作业 <a class="header-anchor" href="#如何抄作业" aria-label="Permalink to “如何抄作业”">​</a></h2><p>举个例子，比如你要把别人的仓库弄一份到自己这来。</p><p><img src="https://image.wuyi.host/posts/20251211002839655.png" alt=""></p><p>点击仓库首页的 <code>克隆/下载</code>，会出现这个，第一行是这个仓库的地址，第二行是克隆这个仓库到本地的命令，其实就是地址前面加了个 <code>git clone</code>。</p><p>一般你要下载到本地，直接复制第二个即可。默认会下载到当前文件夹下，文件夹的名字一般和仓库名相同。</p><p>可以指定路径，比如</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://gitee.com/fwdzh/example.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./test</span></span></code></pre></div><p>会把这个 git 仓库下载到当前目录下的 /test 文件夹。</p><p>此时这个文件夹里是会有 <code>.git</code> 文件夹的，如果你尝试做刚才类似的添加远程仓库的操作，会发现失败了。因为当前仓库已经关联了远程仓库。</p><p>输入</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span></span></code></pre></div><p>可以查看远程仓库信息。</p><p>默认一般是 origin 啥的，我们可以删除掉这个远程连接，但是如果删了在添加自己的，再推送，我们的仓库会有原仓库的提交。因为历史的 <code>commit</code> 信息都是存在 <code>.git</code> 文件夹里的，所以直接删掉那个文件夹就可以了。这样我们的本地文件夹变成了非 git 文件夹。</p><p>再按刚才的 <code>git init</code>，<code>git remote add origin</code> 等操作来一次就好了。</p><h3 id="fork" tabindex="-1">fork <a class="header-anchor" href="#fork" aria-label="Permalink to “fork”">​</a></h3><p>fork 就是把别人的仓库复制一份到我们自己的仓库，并且可以追踪原本的仓库，以后可以拉取更新。</p><p>比如你看到一个仓库比较有意思，想拉取一份自己开发一些新功能或者方便学习，可以使用 fork，fork 的仓库，会显示原本仓库的提交记录，也会显示你的仓库是从哪个仓库 fork 的。</p><h2 id="ssh-连接" tabindex="-1">ssh 连接 <a class="header-anchor" href="#ssh-连接" aria-label="Permalink to “ssh 连接”">​</a></h2><p>如果你的操作系统或者 IDE 无法记忆密码，你可能每次推送代码都需要输入密码，非常麻烦，我们可以使用 ssh 连接。</p><p>以及国内访问 github 可能会很卡，不设置 git 代理经常推送失败 443 端口超时之类的，使用 ssh 可以避免？可能是，不过我一般是挂代理。</p><h3 id="创建公钥" tabindex="-1">创建公钥 <a class="header-anchor" href="#创建公钥" aria-label="Permalink to “创建公钥”">​</a></h3><p>我们首先需要创建并导入 ssh 公钥。</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-keygen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ed25519</span></span></code></pre></div><p>-t 是指定密钥类型，ed25519 的密钥会很短，我用的这个，如果失败了，可以改成 <code>ssh-keygen -t rsa</code>。我以前看的教程都是生成 rsa 来着。</p><p>然后一直回车/yes就行，生成好的密钥在用户文件夹下的 <code>.ssh</code> 文件夹，复制 <code>id_ed22519.pub</code> 里的内容，大概是这个名字吧，rsa 则是 <code>id_rsa</code>。去 gitee/github 的个人设置，找到添加公钥，随便取个名就行。</p><h3 id="ssh-连接-1" tabindex="-1">ssh 连接 <a class="header-anchor" href="#ssh-连接-1" aria-label="Permalink to “ssh 连接”">​</a></h3><p>如果之前已经用 https 连接，需要先删除。</p><p><img src="https://image.wuyi.host/posts/20251211011345000.png" alt=""></p><p>输入 <code>git remote -v</code> 查看远程信息，删除不必要的。</p><p>比如我这个，默认是 origin 是 https 连接的，链接前面有 <code>https</code> 就是 https……</p><p><img src="https://image.wuyi.host/posts/20251211005009857.png" alt=""></p><p>然后再去这里，点上面的 ssh，复制这个链接。</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> master</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;你的链接&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> remote</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span></span></code></pre></div><p>添加远程仓库并查看远程信息，若为 git@ 这样，则连上 ssh 了，以后推送就不需要输入密码了。</p><p>不过由于我们删除了先前的 origin 分支，下一次推送需要使用 <code>git push -u origin master</code>，然后之后还是 <code>git push</code> 就行。</p><p><img src="https://image.wuyi.host/posts/20251211005554712.png" alt=""></p><h2 id="其他" tabindex="-1">其他 <a class="header-anchor" href="#其他" aria-label="Permalink to “其他”">​</a></h2><p><img src="https://image.wuyi.host/posts/20251211005554712.png" alt=""></p><p>GitHub 默认分支是 main，所以可能需要把这里的 <code>git push -u origin master</code> 改成 <code>git push -u orgin main</code>，可能还要用 <code>git branch</code> 切换分支到 main。</p><p>GitHub Pages 可以免费部署静态页面，GitHub Actions 可以自动构建/部署，Cloudflare 和 GitLab 也有类似功能，用这些可以弄一些博客啥的，也都很简单，很多不用写什么代码。</p><p>啊啊啊，好像也没写啥东西，但是花了两小时，而且写出来可能也没啥意义。就这样吧！</p>`,91)])])}const c=s(h,[["render",p]]);export{o as __pageData,c as default};
